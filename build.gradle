allprojects {
    apply plugin: 'idea'
    //apply plugin: 'maven'

    group = 'com.erglog'
    version = '0.0.1-SNAPSHOT'

    repositories {
        //mavenLocal()
        mavenCentral()
        maven {
            url 'http://repo.spray.io'
        }
    }
}
