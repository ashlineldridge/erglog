package com.erglog.domain.workout

import org.joda.time.DateTime
import org.scalatest.{ShouldMatchers, PropSpec}
import org.scalatest.prop.PropertyChecks

class WorkoutSpec extends PropSpec with PropertyChecks with ShouldMatchers {

//  property("Workout should be creatable via its constructor") {
//    forAll { (title: String, comment: String) =>
//      val w = new Workout(title, new DateTime(), comment)
//      w.title should equal (title)
//      w.comment should equal (comment)
//    }
//  }
}